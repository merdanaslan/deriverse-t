{
  "name": "deriverse-trade-history",
  "version": "1.0.0",
  "description": "Retrieve perpetual trading history from Deriverse using TypeScript",
  "main": "perpTradeHistory.ts",
  "scripts": {
    "dev": "ts-node perpTradeHistory.ts",
    "start": "npx ts-node perpTradeHistory.ts",
    "build": "tsc -p tsconfig.json --skipLibCheck",
    "compile": "tsc -p tsconfig.json --skipLibCheck --noEmit false --outDir dist"
  },
  "dependencies": {
    "@deriverse/kit": "^1.0.32",
    "@solana/web3.js": "^1.95.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.3.0"
  },
  "devDependencies": {
    "@types/node": "^18.19.0"
  }
}
